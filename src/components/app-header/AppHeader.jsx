// * styles imports *
import classes from './AppHeader.module.scss';

function AppHeader() {
  return (
    <header className={classes.appHeader}>
      <div>App Header</div>
    </header>
  );
}

export default AppHeader;
